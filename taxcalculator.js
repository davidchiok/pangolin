/*
prompt("")

checking if is a number
1.isNaN
2.Number->
3.+<>







//stay here unit he has entered the right gross
let grossMonthlysalary = Null;

*/
let grossMonthlySalary = null;
while (true){
let gross = prompt("Enter your gross salary");
if (!isNaN(gross)){
    gross = Number(gross) {
        gross = number(gross);
        if (gross > 0) {
            grossMonthlySalary = gross;
            break;
        }
   alert(`invalid amount enetered.
    ensure you entera number greater than 0`)
    }
    `grosss salary is ${grossMonthlySalary} its type ${typeof grossMonthlySalary}`,
}


});

//->here