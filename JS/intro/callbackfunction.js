/Cicle Area Function
//Formula pi * radious
//Arrow
const calculateCicleArea = (radius) ={
return Math.PI * radius * 2;
console. log(calculateCircleArea, "circle", 7);
/ This function calculates the area of a circle. It takes the radius as input and returns the area using the formula pi x radius * 2.
// Rectangle Area Function
// Formula: length x width
const calculateRectangleArea = (length, width) =>{
return length * width;
console. log(calculateRectangleArea, "rectangle", 10, 5);
// This function calculates the area of a rectangle. It takes the length and width, multiplies them, and returns the area.
// Triangle Area Function
// Formula: 0.5 x base x height
const calculateTriangleArea= (base, height) = {
return 0.5 * base * height;
console. log(calculateTriangleArea,
"triangle", 8, 6);
// This function calculates the area of a triangle. It takes the base and height, multiplies them, divides by 2, and returns the area.