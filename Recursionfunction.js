function processDoll(doll) {
    // 1} base case
    if (doll === null) {
        return "no more chocolate here";
    }
    if (doll.hasChocolate) {
        return "yam yam";
    }
    // 2} recursive call to itself
    else if(there are no more dolls)
        return "no more chocolate here :"
} 